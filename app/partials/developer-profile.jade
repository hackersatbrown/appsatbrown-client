div(ng-controller='DeveloperProfileCtrl')
  p {{user.name}}
  p {{user.email}}
  p Apps
  div
    ul.unstyled
      li(ng-repeat='app in user.apps')
        a(href='#/developer/apps/{{app.id}}')
          label(class='checkbox inline')
            {{app.name}}