.row-fluid
  .span3
  .span9
    div(ng-app='ng-app')
      h2 Apps
      div(ng-controller='AppsCtrl')
        ul.unstyled
          li(ng-repeat='app in apps')
            a(href='#/apps/{{app.id}}')
              label(class='inline')
                {{app.name}} - {{app.description}}
